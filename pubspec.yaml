name: maybe_as
description: "Optional as. Returns null if type if not match the class type."
version: 0.0.4
homepage: https://github.com/ios-tech/maybe_as
repository: https://github.com/ios-tech/maybe_as

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter: